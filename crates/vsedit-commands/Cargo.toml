[package]
name = "vsedit-commands"
description = "Command registry and execution"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
vsedit-di = { path = "../vsedit-di" }

