[package]
name = "vsedit-extensions-plat"
description = "Extension manifest and schema"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
vsedit-di = { path = "../vsedit-di" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"

