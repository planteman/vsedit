[package]
name = "vsedit-hash"
description = "Hashing algorithms and UUID generation"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
sha2 = "0.10"
uuid = { version = "1", features = ["v4", "v5"] }

