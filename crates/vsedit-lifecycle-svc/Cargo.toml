[package]
name = "vsedit-lifecycle-svc"
description = "App startup and shutdown lifecycle"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
vsedit-di = { path = "../vsedit-di" }

