[package]
name = "vsedit-terminal-plat"
description = "Terminal PTY abstraction"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
vsedit-di = { path = "../vsedit-di" }
portable-pty = "0.8"
tokio = { version = "1", features = ["process"] }

