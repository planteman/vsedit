[package]
name = "vsedit-wb-keybinding"
description = "User keybinding service"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
vsedit-keybinding-svc = { path = "../vsedit-keybinding-svc" }

