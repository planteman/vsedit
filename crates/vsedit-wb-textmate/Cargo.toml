[package]
name = "vsedit-wb-textmate"
description = "TextMate grammar tokenization"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
vsedit-tokens = { path = "../vsedit-tokens" }
onig = "6"
serde = { version = "1", features = ["derive"] }
serde_json = "1"

